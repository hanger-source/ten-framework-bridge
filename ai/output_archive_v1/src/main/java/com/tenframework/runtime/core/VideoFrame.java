package com.tenframework.runtime.core;

/**
 * Represents a message carrying a single frame of video data.
 */
public interface VideoFrame extends Message {
    // Specific video frame properties like width, height, timestamp, pixel format,
    // etc.
    // will be added here. For now, it's a marker interface.
}