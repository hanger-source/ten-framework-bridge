package com.tenframework.runtime.core;

/**
 * Represents a message carrying a single frame of audio data.
 */
public interface AudioFrame extends Message {
    // Specific audio frame properties like sample rate, channels, bit depth, etc.
    // will be added here. For now, it's a marker interface.
}