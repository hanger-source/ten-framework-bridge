import { IconProps } from "../types"
import { ReactComponent as TranscriptionSvg } from "@/assets/transcription.svg"

export const TranscriptionIcon = (props: IconProps) => {
  return <TranscriptionSvg {...props} />
}
