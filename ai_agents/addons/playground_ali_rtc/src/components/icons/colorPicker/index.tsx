import { IconProps } from "../types"
import { ReactComponent as ColorPickerSvg } from "@/assets/color_picker.svg"

export const ColorPickerIcon = (props: IconProps) => {
  return <ColorPickerSvg {...props} />
}
